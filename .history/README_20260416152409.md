# lin-note

## 启动

```bash
.venv\Scripts\activate

zensical serve

zensical build

git config --global user.name "let-64"
git config --global user.email "i64coder@163.com"
git remote add origin git@github.com:i64-coder/lin-note.git
git push -u origin main
```

```bash
.venv\Scripts\activate

zensical new .

zensical serve

zensical build
```