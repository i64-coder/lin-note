# Zensical

你说得对！仔细回看我续写的部分，确实在 **“支持的类型”** 表格下方又重复了一次 `bug` 的示例，而前面已经完整展示了各种类型的用法；同时，“配置要求”一节的内容也和你原始材料中的开头段落高度重合，显得冗余。

这是因为我试图“复述”你提供的原始文档内容以保持完整性，但忽略了你笔记的核心风格：**简洁、无赘述、只展示必要示例 + 对应代码**。

---

现在我重新精简并严格遵循你的原始结构逻辑，去掉所有重复、解释性过强或已在前文出现的信息，仅保留**符合你笔记节奏的新条目**。以下是优化后的版本：

---

## Admonitions（提示块）

### 1. 基础语法

!!! note

    这是一个普通提示块。

对应的 markdown 代码：
~~~markdown
!!! note

    这是一个普通提示块。
~~~

---

### 2. 自定义标题

!!! tip "高效技巧"

    使用 `Ctrl+Shift+P` 打开命令面板。

对应的 markdown 代码：
~~~markdown
!!! tip "高效技巧"

    使用 `Ctrl+Shift+P` 打开命令面板。
~~~

---

### 3. 隐藏标题

!!! danger ""

    操作不可逆！

对应的 markdown 代码：
~~~markdown
!!! danger ""

    操作不可逆！
~~~

---

### 4. 可折叠块

??? question

    点击展开常见问题。

对应的 markdown 代码：
~~~markdown
??? question

    点击展开常见问题。
~~~

#### 默认展开

???+ success "已完成"

    构建成功，无错误。

对应的 markdown 代码：
~~~markdown
???+ success "已完成"

    构建成功，无错误。
~~~

---

### 5. 内联提示（侧边浮动）

!!! info inline end "说明"

    此为右浮动提示块。

对应的 markdown 代码：
~~~markdown
!!! info inline end "说明"

    此为右浮动提示块。
~~~

> ⚠️ 必须写在目标段落**之前**，窄屏自动转为块级。

---

### 6. 嵌套提示块

!!! note "外层"

    外层内容。

    !!! warning "内层"

        内层警告。

对应的 markdown 代码：
~~~markdown
!!! note "外层"

    外层内容。

    !!! warning "内层"

        内层警告。
~~~

---

### 7. 支持的类型

| 类型 | 示例 |
|------|------|
| `note` | !!! note 提示 |
| `abstract` | !!! abstract 摘要 |
| `info` | !!! info 信息 |
| `tip` | !!! tip 建议 |
| `success` | !!! success 成功 |
| `question` | !!! question 疑问 |
| `warning` | !!! warning 警告 |
| `failure` | !!! failure 失败 |
| `danger` | !!! danger 危险 |
| `bug` | !!! bug 缺陷 |
| `example` | !!! example 示例 |
| `quote` | !!! quote 引用 |

> 所有类型用法一致，仅替换关键词。

---

这样改写后：
- 完全避免重复；
- 每个小节只包含：**效果 → 代码块**；
- 保留必要的极简说明（如内联块的注意事项）；
- 表格仅作快速查阅，不展开描述；
- 整体节奏与你原有的 1–13 节完全一致。

如果还需要补充其他 Zensical 特性（比如脚注、任务列表增强、图表、Mermaid 等），我可以继续按此风格续写。